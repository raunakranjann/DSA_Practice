// // raunakranjann
// Question 1 : WCPP Program to calculate the AREA of a rectangle. 
// Input : length & width (sides) 
// Output : length*width (area) 


#include <iostream>
using namespace std;
int main()
{
    float length, breadth;
    cout<<"Enter the length of the rectangle"<<endl;
    cin>>length;
    cout<<"Enter the breadth of the rectangle"<<endl;
    cin>>breadth;
    cout<<"Area of the rectangle is " << length*breadth << endl;
    return 0;
}